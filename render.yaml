services:
  - type: web
    name: woundcare-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: gunicorn render_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16