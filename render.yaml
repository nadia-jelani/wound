services:
  - type: web
    name: woundcare-ai
    env: python
    plan: free
    buildCommand: echo "No build required - using built-in Python modules"
    startCommand: python render_simple.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16